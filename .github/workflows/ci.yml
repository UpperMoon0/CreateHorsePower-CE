name: CI

on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main
  workflow_dispatch:

# A newer PR head supersedes every older result for that PR. Main keeps its own
# ref-scoped group and seeds Gradle caches that pull requests can restore.
concurrency:
  group: createhorsepower-ci-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  shared-gates:
    name: Shared parity gates
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Set up JDK 21
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '21'

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Make Gradle executable
        run: chmod +x ./gradlew

      # Fail fast on cheap cross-version drift before paying for either full
      # Minecraft toolchain/bootstrap. Configuration-on-demand keeps this root-
      # only file comparison from configuring Forge/NeoForge subprojects.
      - name: Verify shared source and resource parity
        run: ./gradlew verifySharedSources verifySharedResources --configure-on-demand --stacktrace --no-daemon

  platform-runtime:
    name: ${{ matrix.name }} build + GameTests
    needs: shared-gates
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    strategy:
      fail-fast: false
      max-parallel: 2
      matrix:
        include:
          - name: Forge 1.20.1
            project: forge-1.20.1
          - name: NeoForge 1.21.1
            project: neoforge-1.21.1

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Set up JDK 21
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '21'

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Make Gradle executable
        run: chmod +x ./gradlew

      # Build already includes this platform's JUnit suite. Keep it separate
      # from the GameTest invocation so a compile/unit failure never boots a
      # Minecraft server, while the second invocation reuses the same workspace
      # outputs and Gradle user-home state.
      - name: Build and run unit tests
        env:
          PROJECT: ${{ matrix.project }}
        run: ./gradlew ":${PROJECT}:build" --stacktrace --no-daemon

      # One real-game boot per supported loader. All lifecycle/integration
      # scenarios share that single server boot; do not split them into extra
      # jobs/tests because server bootstrap dominates runtime.
      - name: Run Minecraft GameTests
        env:
          PROJECT: ${{ matrix.project }}
        run: ./gradlew ":${PROJECT}:runGameTestServer" --stacktrace --no-daemon

      - name: Upload failure diagnostics
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: gametest-${{ matrix.project }}-logs
          path: |
            ${{ matrix.project }}/run/logs
            ${{ matrix.project }}/build/reports/tests
          if-no-files-found: ignore
          retention-days: 7
